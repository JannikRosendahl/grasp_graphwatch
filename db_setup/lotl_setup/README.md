# LOTL Study - Remove Unknown Executables

This module removes executable processes that are unknown to the training data, as part of the LOTL study dataset preprocessing.

## Prerequisites

**Important:** A normal run must be executed first on your dataset to determine which executables should be removed based on the training data.

